[pytest]
# Включаем автоматическую поддержку asyncio для async def тестов
asyncio_mode = auto

# Ограничиваем поиск тестов папкой tests/ (скрипты в scripts/ не считаем автотестами)
testpaths = tests

# Уменьшаем шум
addopts = -q
